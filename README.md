# Password Generator

A simple Python script that generates a random, secure password based on a length you choose. It mixes uppercase and lowercase letters, digits, and punctuation symbols to create strong, unpredictable passwords.

Features
Generates random passwords of any desired length
Includes uppercase letters, lowercase letters, digits, and special characters
Lightweight — no external dependencies, uses only Python's standard library
